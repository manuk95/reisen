{
  "name": "reisen",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "prebuild": "node scripts/fetch-licensed-images.mjs && node scripts/generate-pwa.mjs",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "test": "node scripts/validate.mjs && node --test tests/*.test.mjs",
    "media": "node scripts/fetch-licensed-images.mjs",
    "pretest": "node scripts/fetch-licensed-images.mjs"
  },
  "dependencies": {
    "@astrojs/check": "latest",
    "@astrojs/mdx": "latest",
    "@astrojs/sitemap": "latest",
    "astro": "latest",
    "leaflet": "^1.9.4",
    "typescript": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "pagefind": "latest"
  }
}
