const CACHE='reisen-v4',BASE='/reisen/';
const CORE=[BASE,BASE+'georgien/',BASE+'georgien/reiseplan/',BASE+'georgien/heute/',BASE+'georgien/wissen/praktisches/',BASE+'georgien/wissen/packliste/',BASE+'georgien/genuss/',BASE+'offline/',BASE+'images/start_image.png',BASE+'images/platzhalter.png',BASE+'manifest.webmanifest'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(CORE)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(event.request.method!=='GET'||url.hostname==='tile.openstreetmap.org'||url.origin!==location.origin)return;event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request).then(response=>{if(response.ok){const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy))}return response}).catch(()=>caches.match(BASE+'offline/'))));});
